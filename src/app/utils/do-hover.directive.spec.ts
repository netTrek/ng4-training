import { DoHoverDirective } from './do-hover.directive';

describe('DoHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DoHoverDirective();
    expect(directive).toBeTruthy();
  });
});
